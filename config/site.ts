export type SiteConfig = typeof siteConfig

export const siteConfig = {
  name: "GFE Foundation Auth Server",
  description:
    "The GFE Foundation Auth Server is a server that provides authentication for the GFE Foundation, a non-profit organization dedicated to promoting and supporting free and open-source software (FOSS) projects. The server uses OAuth 2.0 for authentication and authorization.",
}
